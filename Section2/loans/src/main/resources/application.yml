spring:
  application:
    name: loans
  profiles:
    active:
      "qa"
  config:
    import: "optional:configserver:http://localhost:8071/"
  rabbitmq:
    host: "localhost"
    port: "5672"
    username: "guest"
    password: "guest"

server:
  port: 9091
management:
  endpoints:
    web:
      exposure:
        include: "*"
