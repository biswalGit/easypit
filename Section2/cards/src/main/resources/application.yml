spring:
  application:
    name: cards
  profiles:
    active:
      "qa"
  config:
    import: "optional:configserver:http://localhost:8071/"
  rabbitmq:
    host: "localhost"
    port: "5672"
    username: "guest"
    password: "guest"

server:
  port: 9092
management:
  endpoints:
    web:
      exposure:
        include: "*"
